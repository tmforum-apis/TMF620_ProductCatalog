name: API release

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write

jobs:
  call-api-release-workflow:
    uses: tmforum-apis/tmf-api-commons/.github/workflows/release-api.yml@main
    with:
      api_name: TMF620
